server:
  port: 8100 # 8101, 8102, 8103, ...
spring:
  application:
    name: curconv
  cloud:
    config:
      uri: http://localhost:8888
  profiles:
    active: dev
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
    instance:
      preferIpAddress: true